load("@buildifier_prebuilt//:rules.bzl", "buildifier")
load("@gazelle//:def.bzl", "gazelle")

buildifier(
    name = "buildifier",
    lint_mode = "fix",
    mode = "fix",
)

gazelle(name = "gazelle")
